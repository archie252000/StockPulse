{
    "name": "ottobotassignment",
    "version": "1.0.0",
    "engines": {
        "node": ">=18 <19"
    },
    "description": "",
    "main": "index.js",
    "scripts": {
        "build": "cd server && npm install  && cd ../client && npm install && npx @angular/cli build --configuration production",
        "start": "node server/server.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "fcm-node": "^1.6.1"
    }
}
